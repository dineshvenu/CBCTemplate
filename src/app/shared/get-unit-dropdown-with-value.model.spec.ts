import { GetUnitDropdownWithValue } from './get-unit-dropdown-with-value.model';

describe('GetUnitDropdownWithValue', () => {
  it('should create an instance', () => {
    expect(new GetUnitDropdownWithValue()).toBeTruthy();
  });
});
