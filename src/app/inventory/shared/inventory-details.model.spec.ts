import { InventoryDetails } from './inventory-details.model';

describe('InventoryDetails', () => {
  it('should create an instance', () => {
    expect(new InventoryDetails()).toBeTruthy();
  });
});
