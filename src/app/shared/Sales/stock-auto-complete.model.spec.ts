import { StockAutoComplete } from './stock-auto-complete.model';

describe('StockAutoComplete', () => {
  it('should create an instance', () => {
    expect(new StockAutoComplete()).toBeTruthy();
  });
});
